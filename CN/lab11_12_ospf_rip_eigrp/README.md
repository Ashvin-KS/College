# Combined OSPF, RIP, and EIGRP Packet Tracer Labs

This folder contains the packet tracer files for the combined routing experiments.

## Contents

| File | Purpose |
| --- | --- |
| `OSPF_Lab.pkt` | OSPF routing scenario |
| `RIP_Lab.pkt` | RIP routing scenario |
| `EIGRP_Lab.pkt` | EIGRP routing scenario |

## Notes

- The accompanying report text and screenshots are in [pdf_report](pdf_report).
- Keep the `.pkt` files together so the routing experiments stay easy to open in Packet Tracer.